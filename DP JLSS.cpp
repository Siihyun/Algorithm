#include<iostream>
#include<cstring>
#include<algorithm>
#include<vector>
int cache[100];
vector<int> A;
vector<int> B;
int cache[100];
int find(int idx1,int idx2){
	int& ret = cache[idx];
    if(ret = -1)
    	return ret;
    ret = 1;
    for(int start = idx; i < len_A ; start++){
    	if(idx)	
	}
    
   
}
int main(){
    int testcase;
    int len_A,len_B;
    int tmp;
    while(testcase--){
    	memset(cache,-1,sizeof(cache));
    	A.clear();
    	B.clear();
   		cin>>len_A>>len_B;
    	for(int i = 0 ; i< len_A; i++){
    		cin>>tmp;
    		A.push_back(tmp);
    	}
		for(int i = 0 ; i< len_B; i++){
    		cin>>tmp;
    		B.push_back(tmp);
    	}
   	
    int sol;
        for(int i = 0 ; i< len_A ; i++){
        	sol = find(0,A);
      
      
   }
}
