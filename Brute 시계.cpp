#include<iostream>
const int INF = 9999, SWITCHES = 10, CLOCKS = 16;
int main(){
	const char linked[SWITCHES][CLOCKS] = {
		"xxx............"
	}
}
bool areAligned(const vector<int>& clocks) // check if every clock is set
