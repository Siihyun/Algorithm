#include<iostream>
int main(){
	int testcase;
	cin>>testcase;
	
	while(testcase--){
		cin>>str;
		for(int i = 0; i < strlen(str); i++){
			while(str[i] != x)
				i++;
			swap(str[i],str[i+1]);
			swap(str[i+2],str[i+3]);
		}
		print(str);
		}
	}
}
void change(){
	
}
